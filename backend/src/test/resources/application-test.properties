spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;NON_KEYWORDS=USER
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

spring.security.user.name=test
spring.security.user.password=test

# ── Test doubles for values normally supplied by application.properties ──────
# The real application.properties is gitignored (it holds secrets), so the test
# context must be self-sufficient: these dummies exist only so beans can start.
app.upload-dir=target/test-uploads/
app.frontend-url=http://localhost:5173
app.cors.allowed-origins=http://localhost:5173

jwt.secret=VGVzdE9ubHlTZWNyZXRLZXlGb3JKV1RUZXN0aW5nUHVycG9zZXMxMjM0NTY=
jwt.expiration-ms=86400000

spring.mail.host=localhost
spring.mail.port=2525
spring.mail.username=test@careerflow.local
spring.mail.password=test

spring.security.oauth2.client.registration.google.client-id=test-client-id
spring.security.oauth2.client.registration.google.client-secret=test-client-secret
spring.security.oauth2.client.registration.google.scope=email,profile
