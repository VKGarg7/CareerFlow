VITE_API_URL=http://localhost:8080/api

# Optional: shows a "try the demo" banner with these creds pre-filled on the login page.
# Leave blank/unset to hide the banner.
VITE_DEMO_EMAIL=
VITE_DEMO_PASSWORD=
